# LICSDE NeurIPS 2026 reproducibility snapshot — pinned to the
# environment used to produce all results in the paper (DJIA-28,
# DJIA-28, 2018-2023). Tested on Python 3.12 + Linux.
#
# Install:   pip install -r requirements.snapshot.txt
# (or use the conda recipe in environment.yml)

# ── Core numerics ───────────────────────────────────────────────────
numpy>=2.0,<3.0          # >=2.0 required for np.trapezoid (TSTR benchmark)
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3

# ── Deep learning (CPU works; GPU optional) ─────────────────────────
torch>=2.0

# ── Plotting ────────────────────────────────────────────────────────
matplotlib>=3.7
plotly>=5.15

# ── Config / IO ─────────────────────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0
tqdm>=4.65
requests>=2.28

# ── Time-series utilities ───────────────────────────────────────────
statsmodels>=0.14
arch>=6.0                # GARCH stylized-facts metric

# ── Market data (only needed for re-downloading raw OHLCV) ──────────
yfinance>=0.2.18

# ── LLM / RAG (only needed if RE-extracting factors;
#               cached factors in cache/djia28/ avoid this) ─
litellm>=1.0
chromadb>=0.4
sentence-transformers>=2.2
fredapi>=0.5             # FRED macro data (optional)
